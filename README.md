# balloon-detection
Real-time balloon detection system using YOLOv8 with custom dataset preprocessing. The model detects balloons with bounding boxes and confidence scores, while color and shape are extracted using classical computer vision techniques.
